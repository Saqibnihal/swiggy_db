
const uuidField = { type: 'string', format: 'uuid' };

const timestamps = { type: "string", format: "date-time" };

const string = { type: "string" };

const number = { type: "number" };

module.exports = { uuidField, timestamps, string, number }